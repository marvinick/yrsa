<div class="container">
  <% @account.projects.each do |project| %>
    <%= link_to project.title, account_project_path(project) %>
  <% end %>
</div>
<%= link_to "New Project", new_account_project_path(@account) %>
