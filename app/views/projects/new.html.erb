<div class="row">
  <div class="col-md-2"></div>
  <div class="col-md-8">
    <h1 class="text-center "><%= link_to @account.name, account_path(@account) %></h1>
  </div>
  <div class="col-md-2"></div>
</div>

<%= form_for([current_account, @project]) do |f| %>
<div class="row">
  <div class="col-md-2"></div>
  <div class="col-md-8">
    <div class="formly text-center">
      <h2>new project</h2>

        <br>
        <div class="control">
            <%= f.text_field :title, placeholder: 'title', label: false, class:"input is-medium" %>
        </div>
        <br>
        <%= f.text_area :description, placeholder: 'write away', label: false, class:"input is-medium" %>
        <br>
        <br>
        <br>
        <div class="row">
          <div class="col-md-3"></div>
          <div class="col-md-6 text-center">
            <h5>Add your team members here:</h5>
            <%= f.collection_check_boxes :user_ids, current_account.users.all, :id, :email, :item_wrapper_class => 'checkbox_container' do |cb| %>
              <% cb.label {cb.check_box(class: "checkbox") + cb.text} %>
            <% end %>
          </div>
          <div class="col-md-3"></div>
        </div>
    </div>
  </div>
  <div class="col-md-2"></div>
</div>

<div class="text-center">
  <%= f.submit "yes please", class: "btn btn-default btn-lg" %>
  <%= link_to "never mind", :back, {:class=>"btn btn-lg btn-default"} %>
</div>
<% end %>
<br>
