<h2><%= @account.name %></h2>
<% @account.projects.each do |project| %>
  <h3><%= link_to project.title, account_project_path(@account, project) %></h3>
<% end %>

  <li><%= link_to "Change Plan", account_choose_plan_path(@account) %></li>
  <li><%= link_to "Users", account_users_path(@account, @users) %></li>
  <li><%= link_to "Invitations", account_invitations_path(@account, @invitations) %></li>
  <h6><%= link_to "New Project", new_account_project_path(@account) %></h6>
