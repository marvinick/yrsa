<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <li class="navbar-brand" href="#"><%= link_to "aetomata", home_path %></li>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
      </ul>

      <ul class="nav navbar-nav navbar-center">
        <% if user_signed_in? %>
          <li> <%= link_to "home", root_url %></li>
          <% if controller_name != "activities" %>
            <li><%= link_to "activity", activities_path %></li>
          <% end %>

          <% if controller_name != "lookups" %>
            <% if @account %>
              <li><%= link_to "search", account_lookups_path(@account) %></li>
            <% elsif current_account %>
              <li><%= link_to "search", account_lookups_path(current_account) %></li>
            <% end %>
          <% end %>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <icon class="fa fa-user"></icon>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
                <li><<%= link_to "Admin", admin_root_path %></li>
              <% end %>
              <li><a href="#"><%= current_user.email %></a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">  <%= link_to "logout", destroy_user_session_path, method: :delete %></a></li>
            </ul>
          </li>

        <% else %>
          <li><%= link_to "signup", new_account_path %></li>
          <li><%= link_to "signin", new_user_session_path %></li>
        <% end %>
      </ul>

    </div>
  </div>
</nav>
