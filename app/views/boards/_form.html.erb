
<%= form_for ([current_account, set_project, @board]), remote: true, html: {} do |f| %>
  <div class="invitation-form" id="current-pane">
    <% f.hidden_field :project_id %>
    <%= f.text_field :note, placeholder: "name", class: "input-text" %>
    <br>
    <%= f.button "add", class: "btn btn-success" %>
    <button href="#current-pane" class="btn btn-xs" data-dismiss="alert" aria-label="close" id="hide">close</button>
  </div>
<% end %>
