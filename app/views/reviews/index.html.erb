

<div class="row">
  <% set_item.reviews.each do |review| %>
  <div class="box">
    <% review.properties.each do |name, value| %>
      <li><%= name %> : <%= value %></li>
    <% end %>
    <%= link_to "edit", edit_account_project_item_review_path(current_account, set_project, @item, review) %>

  </div>

  <% end %>
</div>
